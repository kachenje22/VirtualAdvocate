
@{
   // ViewBag.Title = "CompareDocument";
    Layout = "~/Views/Shared/_Layoutmain.cshtml";
}

<h2>Compare Document</h2>

@using (Html.BeginForm("CompDocument", "DocumentManagement", FormMethod.Post, new { @class = "form-horizontal", onsubmit = "showProgress()" }))
{
    @Html.AntiForgeryToken()
    <div class="row">
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="col-md-1">
                    <input type="submit" class="btn btn-default" value="Compare" id="btnSubmit" />
                </div>
            </div>
        </div>
}